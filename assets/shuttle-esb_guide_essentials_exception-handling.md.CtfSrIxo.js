import{_ as e,c as s,o as i,a4 as n}from"./chunks/framework.BvIE6Ka-.js";const g=JSON.parse('{"title":"Exception Handling","description":"","frontmatter":{},"headers":[],"relativePath":"shuttle-esb/guide/essentials/exception-handling.md","filePath":"shuttle-esb/guide/essentials/exception-handling.md"}'),t={name:"shuttle-esb/guide/essentials/exception-handling.md"},a=n(`<h1 id="exception-handling" tabindex="-1">Exception Handling <a class="header-anchor" href="#exception-handling" aria-label="Permalink to &quot;Exception Handling&quot;">​</a></h1><p>When an exception occurs within a pipeline an <code>OnPipelineException</code> event is raised on the pipeline and any observers that have hooked onto the event will be called:</p><div class="language-c# vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">c#</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> class</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ReceiveExceptionObserver</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> : </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">IPipelineObserver</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">OnPipelineException</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Execute</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">OnPipelineException</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> pipelineEvent</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        // set by calling MarkExceptionHandled</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (pipelineEvent.Pipeline.ExceptionHandled) </span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">            return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        }</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        // sets ExceptionHandled to true</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        pipelineEvent.Pipeline.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">MarkExceptionHandled</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(); </span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        // prevents further processing of the pipeline</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        pipelineEvent.Pipeline.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Abort</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(); </span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Typically you would not respond to the <code>OnPipelineException</code> event but you need to be cognizant of how it affects the message processing.</p><p>Exception handling differs between queues and streams.</p><h2 id="queues" tabindex="-1">Queues <a class="header-anchor" href="#queues" aria-label="Permalink to &quot;Queues&quot;">​</a></h2><p>The more interesting bit is that the first order of business is to determine whether or not to retry the message. If the exception is of type <code>UnrecoverableHandlerException</code> the message will not be retried and will be immediately moved to the <code>ErrorQueue</code>. This means that when you are able to determine that the message will never be processed correctly, but you do not want to discard it outright, you can throw this exception and the message will be moved directly to the <code>ErrorQueue</code>. There is also an option to set the <code>IHandlerContext.ExceptionHandling</code> to <code>ExceptionHandling.Poison</code> which will also move the message directly to the <code>ErrorQueue</code>. In instances where no <code>ErrorQueue</code> has been defined the message will simply be released. In most <code>IQueue</code> implementations this will leave the message at the head of the queue and it will immediately become available for processing again. This would lead to your queue processing being blocked by this message which is, of course, less than ideal and the message should be handled or acknowledged/ignored in some way.</p><p>If the exception is not of type <code>UnrecoverableHandlerException</code> the implementation of the <code>IServiceBusPolicy</code> is used to invoke the relevant policy method. The <code>DefaultServiceBusPolicy</code> makes use of the <code>ServiceBusOptions</code> to determine the <code>MaximumFailureCount</code>. Should the number of failed messages still be within this count the message will be retried and duration to wait until the next retry is determined by using the <code>DurationToIgnoreOnFailure</code> value.</p><p>Should the message be retried the exception message is added to the <code>FailureMessages</code> collection of the <code>TransportMessage</code> and the transport message is re-enqueued, moving it to the end of the queue and the <code>IgnoreTillDate</code> property of the <code>TransportMessage</code> is also set to the duration to wait before retrying. It is also possible to set the <code>IHandlerContext.ExceptionHandling</code> to <code>ExceptionHandling.Retry</code> to explicitly retry the message when you encounter an exception.</p><p><strong>Note</strong>: you may experience queue thrashing if you do not have a <code>DeferredQueue</code> configured for your inbox and mesasges are retried. When the inbox processor dequeues the message it determines whether it can be processed by checking the <code>IgnoreTillDate</code> value. Should the <code>IgnoreTillDate</code> be in the future (deferred) it is moved to the <code>DeferredQueue</code> if there is one; else it is simply re-enqueued in the inbox which moves it to the back of the queue. Since the inbox is responsible for processing messages as quickly as possible this processing will continue until the message again becomes available for processing. Given that there is a message to process in the inbox no backing-off will occur. Please always use a deferred queue for anything that will be executed in a production environment.</p><h2 id="streams" tabindex="-1">Streams <a class="header-anchor" href="#streams" aria-label="Permalink to &quot;Streams&quot;">​</a></h2><p>Exception handling for streams is mostly the same as for queues except that no retries are permitted. The reason being that a stream is not specific to any specific consumer and producing another message will immediately result in duplicate processing.</p><p>Message sent to streams also may not be deferred.</p>`,13),o=[a];function l(h,r,p,d,c,u){return i(),s("div",null,o)}const E=e(t,[["render",l]]);export{g as __pageData,E as default};
